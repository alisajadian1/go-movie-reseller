syntax = "proto3";

package userpb;

option go_package = "./internal/proto/userpb";

message User {
  uint64 id       = 1;
  string name     = 2;
  string username = 3;
}

message UserId {
  uint64 userId = 1;
}

service UserService {
  rpc GetUser(UserId) returns (User);
}